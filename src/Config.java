public class Config {

    public static String tag = "pre";     //Tag to print before and after the text
    public static String suffix = ":";    //Optional suffix, leave blank if not needed
    public static boolean italics = true; //Makes commands italic if set to true
    public static boolean bold = false;   //Makes commands bold if set to true
    public static int minSpaceCount = 3;  //Minimum spaces definition should be from command
}